import { Routes } from '@angular/router';
import { LoginAndRegisterComponent } from './pages/login-and-register/login-and-register.component';

export const routes: Routes = [
    {path:'sign',component:LoginAndRegisterComponent}
];
